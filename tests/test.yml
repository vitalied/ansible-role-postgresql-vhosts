---
# test file for hswong3i.postgresql_vhosts

- hosts: localhost
  remote_user: root
  roles:
    - hswong3i.postgresql
    - role: ansible-role-postgresql-vhosts
      postgresql_vhosts_id: "example"
      postgresql_vhosts_pass: "xaivoo9Z"
      postgresql_vhosts_user: "example"
